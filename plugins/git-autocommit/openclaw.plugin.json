{
  "id": "git-autocommit",
  "name": "Git AutoCommit",
  "description": "Stages and commits repo changes before every OpenClaw agent turn. Set repoDir in plugin config or GIT_AUTOCOMMIT_REPO env.",
  "activation": {
    "onStartup": true
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "repoDir": {
        "type": "string",
        "description": "Path to the git repository to auto-commit (defaults to agent workspace)"
      }
    }
  }
}
